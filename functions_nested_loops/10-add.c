#include <stdio.h>
#include "main.h"
/**
 * add - will add 2 integers
 *
 * @a - is an integer in the code
 * @b - is an integer in the code
 * return: a + b
 */
int add(int a, int b)
{
	return (a + b);
}
